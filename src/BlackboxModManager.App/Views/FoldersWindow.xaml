<Window x:Class="BlackboxModManager.App.Views.FoldersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Folders" Width="820" Height="460" MinWidth="520" MinHeight="300"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,10"
               Text="Every directory that this application reads or writes. Open opens the directory in the file manager of the platform. Copy path puts the path on the clipboard, which always works." />

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <ItemsControl x:Name="Rows">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border BorderBrush="#40808080" BorderThickness="0,0,0,1" Padding="0,0,0,6" Margin="0,0,0,6">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                  <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                  <TextBlock Text="{Binding Purpose}" Style="{StaticResource Hint}" TextWrapping="Wrap" />
                  <!-- The path is selectable, so a user can copy part of it by hand. -->
                  <TextBox Text="{Binding Path, Mode=OneWay}" IsReadOnly="True" BorderThickness="0"
                           Background="Transparent" Padding="0" Margin="0,2,0,0"
                           TextWrapping="Wrap" />
                  <TextBlock Text="{Binding State}" Style="{StaticResource Hint}" TextWrapping="Wrap"
                             Margin="0,2,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                  <Button Content="Open" Click="OnOpen" Tag="{Binding}" MinWidth="70"
                          IsEnabled="{Binding Exists}" />
                  <Button Content="Copy path" Click="OnCopy" Tag="{Binding}" MinWidth="90" Margin="0" />
                </StackPanel>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <Grid Grid.Row="2" Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock x:Name="Report" Grid.Column="0" VerticalAlignment="Center" TextWrapping="Wrap"
                 Style="{StaticResource Hint}" Margin="0,0,8,0" />
      <Button Grid.Column="1" Content="Close" IsDefault="True" IsCancel="True" MinWidth="80" Margin="0" />
    </Grid>
  </Grid>
</Window>
